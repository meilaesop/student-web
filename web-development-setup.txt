项目配置说明（精简版）

一、项目概览
- 项目：D:\Microsoft VS Code\student-web
- GitHub 仓库：https://github.com/meilaesop/student-web.git
- 目标：本地完成前端学习环境配置，确保项目可正常运行与提交。

二、基础信息
- Git：D:\Git\cmd\git.exe，版本 2.55.0.windows.5
- Python：D:\Python\python.exe
- Node.js：D:\nodejs，版本 v24.21.0
- GitHub 用户名：meilaesop
- 提交邮箱：meilaesop@gmail.com

三、环境自检
在 PowerShell 中依次执行：
- git --version
- python --version
- python -m pip --version
- node --version
- npm --version

若报错"无法将 xxx 项识别为 cmdlet"，重启 VS Code 即可（绝大多数是终端会话过期，PATH 已配置正确，无需重配）。

四、PATH 配置（仅重启 VS Code 后仍报错时才需要，用方案 B）
用命令行写入当前用户级变量，避免动到系统级变量：
  $paths = "D:\Git\cmd","D:\Python","D:\Python\Scripts","D:\nodejs"
  $cur = [Environment]::GetEnvironmentVariable("Path","User")
  $list = $cur.Split(';') | Where-Object { $_ -ne '' }
  foreach ($p in $paths) {
      if ($list -notcontains $p) { $list += $p }
  }
  [Environment]::SetEnvironmentVariable("Path", ($list -join ';'), "User")
执行后重启 VS Code，用 where git 验证（应输出 D:\Git\cmd\git.exe）。

五、Git 全局身份
执行：
- git config --global user.name "meilaesop"
- git config --global user.email "meilaesop@gmail.com"

六、Node.js 与 npm
执行：
- Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned -Force
- node --version
- npm --version

七、项目 Git 仓库
若项目目录无 .git，执行：
- git init
当前阶段不要自动提交。

八、Continue 与 DeepSeek
- 在 VS Code 扩展市场安装 Continue 扩展。
- 到 https://platform.deepseek.com/api_keys 创建 API Key。
- Continue 中添加 Chat model，Provider 选 DeepSeek，Model 选 deepseek-chat，填入 Key 并 Connect。
- 安全要求：API Key 不写入项目文件、README 或任何代码文件，不提交到 GitHub。

九、最终验证
逐项执行：
- git --version
- python --version
- python -m pip --version
- node --version
- npm --version
- node --check "D:\Microsoft VS Code\student-web\script.js"
- git config --global user.name
- git config --global user.email
- git -C "D:\Microsoft VS Code\student-web" status

十、完成标准
以下都满足即配置完成：
- 所有命令成功运行，路径与版本正确；
- Git 身份已配置；
- Node.js 与 npm 正常；
- JavaScript 语法检查无报错；
- DeepSeek 已连接。

